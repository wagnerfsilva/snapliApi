{
  "name": "fotow-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for processing uploaded photos",
  "main": "index.js",
  "scripts": {
    "deploy": "zip -r function.zip . && aws lambda update-function-code --function-name fotow-image-processor --zip-file fileb://function.zip"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.478.0",
    "@aws-sdk/client-rekognition": "^3.478.0",
    "sharp": "^0.33.1"
  }
}
